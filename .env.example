# Neon Postgres. Pooled URL for the app, direct URL for migrations.
DATABASE_URL=
DIRECT_URL=
# Any long random string; signs the session cookies.
AUTH_SECRET=
# First admin, created by `npm run db:seed` if no admin exists yet.
ADMIN_EMAIL=
ADMIN_NAME=
ADMIN_PASSWORD=
